<project>
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <artifactId>takari</artifactId>
    <groupId>io.takari</groupId>
    <version>44</version>
  </parent>
  <groupId>ca.vanzyl</groupId>
  <artifactId>harbor-client</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>takari-jar</packaging>

  <properties>
    <harbor.version>2.3.2</harbor.version>
    <harbor.api.version>2.0</harbor.api.version>
    <takari.privatePackageReference>ignore</takari.privatePackageReference>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Dependencies generated by the maven-swagger-codegen-plugin -->
    <swagger.version>1.6.2</swagger.version>
    <!-- OkHttp -->
    <gson-fire-version>1.8.3</gson-fire-version>
    <okhttp-version>2.7.5</okhttp-version>
    <gson-version>2.8.1</gson-version>
    <!-- Testing -->
    <junit.version>5.8.0</junit.version>
    <assertj.version>3.21.0</assertj.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>io.swagger</groupId>
      <artifactId>swagger-annotations</artifactId>
      <version>${swagger.version}</version>
    </dependency>
    <!-- javax.annotation.Generated -->
    <dependency>
      <groupId>org.jboss.spec.javax.annotation</groupId>
      <artifactId>jboss-annotations-api_1.2_spec</artifactId>
      <version>1.0.2.Final</version>
    </dependency>
    <!-- OKHttp -->
    <dependency>
      <groupId>com.squareup.okhttp</groupId>
      <artifactId>okhttp</artifactId>
      <version>${okhttp-version}</version>
    </dependency>
    <dependency>
      <groupId>com.squareup.okhttp</groupId>
      <artifactId>logging-interceptor</artifactId>
      <version>${okhttp-version}</version>
    </dependency>
    <dependency>
      <groupId>com.google.code.gson</groupId>
      <artifactId>gson</artifactId>
      <version>${gson-version}</version>
    </dependency>
    <dependency>
      <groupId>io.gsonfire</groupId>
      <artifactId>gson-fire</artifactId>
      <version>${gson-fire-version}</version>
    </dependency>
    <!-- test dependencies -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-params</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-engine</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>${assertj.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.github.hakky54</groupId>
      <artifactId>sslcontext-kickstart</artifactId>
      <version>7.0.0</version>
    </dependency>
    <dependency>
      <groupId>io.github.hakky54</groupId>
      <artifactId>sslcontext-kickstart-for-pem</artifactId>
      <version>7.0.0</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>io.swagger</groupId>
        <artifactId>swagger-codegen-maven-plugin</artifactId>
        <version>2.4.21</version>
        <configuration>
          <inputSpec>${project.basedir}/src/main/swagger/harbor-swagger-${harbor.version}.yaml</inputSpec>
          <language>java</language>
          <library>okhttp-gson</library>
          <apiPackage>ca.vanzyl.harbor.client.okhttp.api</apiPackage>
          <modelPackage>ca.vanzyl.harbor.client.okhttp.model</modelPackage>
          <invokerPackage>ca.vanzyl.harbor.client.okhttp.invoker</invokerPackage>
          <generateSupportingFiles>true</generateSupportingFiles>
          <generateModelTests>false</generateModelTests>
          <generateModelDocumentation>false</generateModelDocumentation>
          <generateApiTests>false</generateApiTests>
          <generateApiDocumentation>false</generateApiDocumentation>
          <configOptions>
            <!-- We are specifically using the JDK time API: https://blog.joda.org/2009/11/why-jsr-310-isn-joda-time_4941.html -->
            <dateLibrary>java8</dateLibrary>
          </configOptions>
        </configuration>
        <!--
        <executions>
          <execution>
            <goals>
              <goal>generate</goal>
            </goals>
          </execution>
        </executions>
        -->
      </plugin>
    </plugins>
  </build>
</project>
